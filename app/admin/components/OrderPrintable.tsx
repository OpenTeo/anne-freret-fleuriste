'use client';

// This component is rendered via OrderDetail's handlePrint (window.open).
// Kept as a placeholder for direct usage if needed.

export default function OrderPrintable() {
  return null;
}
